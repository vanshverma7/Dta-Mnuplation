That makes sense, and it lines up with what we were seeing. The 31.5k disagreements between MI and DI weren't random: in every case the MI file was almost exactly 2x the DI value, which is the kind of systematic pattern you'd expect from two different aggregations of the same underlying field (e.g. gross claim vs net/paid) being labelled identically as TFRAMT.
So the headline is probably less about accuracy and more about the field being mislabelled in practice. Users reading TFRAMT as "amount paid" would actually be working off claim values. Agree it's worth getting total_gross_payment_amount into the extract if we can, and being upfront with users in the meantime.
Happy to help draft the user-facing note when you're ready, or do a quick reconciliation once the payment amount is added to confirm how it compares. I can also quickly quantify the impact (volume and total £ value of the gap between MI and DI) if that would be useful to have when going back to users.

